# 🤝 Руководство для разработчиков

Спасибо за интерес к проекту MAX Bot! Это руководство поможет вам начать разработку.

## 📋 Содержание

- [Начало работы](#начало-работы)
- [Структура проекта](#структура-проекта)
- [Стандарты кодирования](#стандарты-кодирования)
- [Git workflow](#git-workflow)
- [Тестирование](#тестирование)
- [Pull Request процесс](#pull-request-процесс)

---

## 🚀 Начало работы

### Требования

- **Node.js**: 18.x или выше
- **npm**: 9.x или выше
- **Docker**: 20.10+ (для локальной разработки)
- **Git**: 2.x или выше
- **PostgreSQL**: 15+ (если запускаете локально без Docker)
- **Redis**: 7+ (если запускаете локально без Docker)

### Настройка окружения разработки

#### 1. Fork и клонирование репозитория

```bash
# Fork репозитория на GitHub
# Затем клонируйте свой fork

git clone https://github.com/YOUR_USERNAME/max_bot.git
cd max_bot

# Добавьте upstream remote
git remote add upstream https://github.com/ORIGINAL_OWNER/max_bot.git
```

#### 2. Установка зависимостей

```bash
# API
cd api
npm install
cd ..

# Bot
cd bot
npm install
cd ..

# Web
cd web
npm install
cd ..
```

#### 3. Настройка окружения

```bash
# Скопируйте example-env в .env
cp example-env .env

# Отредактируйте .env для локальной разработки
nano .env
```

#### 4. Запуск инфраструктуры

```bash
# Запустите PostgreSQL и Redis
docker-compose up -d postgres redis

# Или запустите все сервисы
docker-compose up -d
```

#### 5. Запуск в режиме разработки

```bash
# В терминале 1: API
cd api
npm run dev

# В терминале 2: Bot
cd bot
npm run dev

# В терминале 3: Web
cd web
npm run dev
```

---

## 📁 Структура проекта

### API Server (`/api`)

```
api/
├── src/
│   ├── config/              # Конфигурация (БД, Swagger)
│   ├── controllers/         # Контроллеры для обработки HTTP запросов
│   ├── database/            # Миграции и инициализация БД
│   ├── middleware/          # Express middleware
│   ├── repositories/        # Репозитории для работы с БД
│   ├── routes/              # API маршруты
│   ├── services/            # Бизнес-логика
│   ├── types/               # TypeScript типы и интерфейсы
│   ├── utils/               # Вспомогательные функции
│   └── index.ts            # Точка входа
├── public/                  # Статические файлы
├── Dockerfile
├── package.json
└── tsconfig.json
```

### Bot (`/bot`)

```
bot/
├── src/
│   ├── commands/           # Команды бота (/start, etc)
│   ├── handlers/           # Обработчики событий
│   ├── i18n/               # Локализация (ru.json, en.json)
│   ├── keyboards/          # Клавиатуры для бота
│   ├── middlewares/        # Middleware для обработки сообщений
│   ├── services/           # Сервисы (API клиент, авторизация)
│   ├── types/              # TypeScript типы
│   └── bot.ts             # Точка входа
├── Dockerfile
├── package.json
└── tsconfig.json
```

### Web Interface (`/web`)

```
web/
├── src/
│   ├── components/         # React компоненты
│   │   ├── ui/            # UI компоненты (Button, Input, etc)
│   │   └── ...            # Бизнес-компоненты
│   ├── pages/              # Страницы приложения
│   ├── services/           # API клиенты
│   ├── types/              # TypeScript типы
│   ├── lib/                # Вспомогательные функции
│   ├── App.tsx            # Главный компонент
│   └── main.tsx           # Точка входа
├── public/                 # Статические файлы
├── Dockerfile
├── package.json
├── vite.config.ts
└── tsconfig.json
```

---

## 💻 Стандарты кодирования

### TypeScript

Мы используем TypeScript во всех частях проекта.

**Основные правила:**

1. **Типизация**: Всегда указывайте типы для функций и переменных
```typescript
// ✅ Хорошо
function getUserById(id: number): Promise<User> {
  // ...
}

// ❌ Плохо
function getUserById(id) {
  // ...
}
```

2. **Интерфейсы vs Types**: Используйте `interface` для объектов, `type` для union/intersection
```typescript
// ✅ Хорошо
interface User {
  id: number;
  name: string;
}

type Status = 'pending' | 'approved' | 'rejected';

// ❌ Плохо
type User = {
  id: number;
  name: string;
}
```

3. **Nullable типы**: Явно указывайте nullable типы
```typescript
// ✅ Хорошо
function findUser(id: number): User | null {
  // ...
}

// ❌ Плохо
function findUser(id: number): User {
  return null; // Type error!
}
```

### Именование

- **Файлы**: `kebab-case.ts` для обычных файлов, `PascalCase.tsx` для React компонентов
- **Классы**: `PascalCase`
- **Функции/переменные**: `camelCase`
- **Константы**: `UPPER_SNAKE_CASE`
- **Интерфейсы**: `PascalCase` (без префикса `I`)

```typescript
// ✅ Хорошо
const MAX_RETRIES = 3;
interface User { ... }
class UserService { ... }
function getUserById() { ... }

// ❌ Плохо
const max_retries = 3;
interface IUser { ... }
class userService { ... }
function GetUserById() { ... }
```

### Форматирование

Проект использует единые настройки форматирования:

```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "useTabs": true,
  "trailingComma": "es5"
}
```

Перед коммитом запустите:
```bash
npm run format  # если есть
```

### Комментарии

- Пишите комментарии на русском или английском языке
- Используйте JSDoc для документирования функций

```typescript
/**
 * Получает пользователя по ID
 * @param id - ID пользователя
 * @returns Promise с данными пользователя или null
 */
async function getUserById(id: number): Promise<User | null> {
  // Реализация...
}
```

---

## 🌿 Git Workflow

Мы используем Git Flow workflow.

### Ветки

- `main` - Production код (защищена)
- `develop` - Development код
- `feature/*` - Новые функции
- `bugfix/*` - Исправление багов
- `hotfix/*` - Срочные исправления для production

### Создание новой feature

```bash
# Обновите develop
git checkout develop
git pull upstream develop

# Создайте feature ветку
git checkout -b feature/add-certificate-download

# Разработка...
git add .
git commit -m "feat: add certificate download functionality"

# Push в свой fork
git push origin feature/add-certificate-download

# Создайте Pull Request на GitHub
```

### Commit сообщения

Мы используем [Conventional Commits](https://www.conventionalcommits.org/):

**Формат:**
```
<type>(<scope>): <subject>

<body>

<footer>
```

**Типы:**
- `feat`: Новая функциональность
- `fix`: Исправление бага
- `docs`: Изменения в документации
- `style`: Форматирование, отсутствие изменений в коде
- `refactor`: Рефакторинг кода
- `test`: Добавление или изменение тестов
- `chore`: Изменения в build процессе, зависимостях

**Примеры:**

```bash
# Новая функция
git commit -m "feat(api): add certificate download endpoint"

# Исправление бага
git commit -m "fix(bot): fix authentication error handling"

# Документация
git commit -m "docs: update API reference"

# Рефакторинг
git commit -m "refactor(web): extract certificate card to component"
```

---

## 🧪 Тестирование

### Запуск тестов

```bash
# API
cd api
npm test

# Bot
cd bot
npm test

# Web
cd web
npm test
```

### Написание тестов

#### Unit тесты (Jest)

```typescript
// user.service.test.ts
import { UserService } from './user.service';

describe('UserService', () => {
  let service: UserService;

  beforeEach(() => {
    service = new UserService();
  });

  describe('getUserById', () => {
    it('should return user when found', async () => {
      const user = await service.getUserById(1);
      expect(user).toBeDefined();
      expect(user.id).toBe(1);
    });

    it('should return null when user not found', async () => {
      const user = await service.getUserById(999);
      expect(user).toBeNull();
    });
  });
});
```

#### Integration тесты

```typescript
// certificate.routes.test.ts
import request from 'supertest';
import app from '../app';

describe('Certificate Routes', () => {
  let token: string;

  beforeAll(async () => {
    // Получить токен для тестов
    const response = await request(app)
      .post('/api/auth/login')
      .send({ email: 'test@test.com', password: 'test123' });
    token = response.body.data.tokens.access_token;
  });

  describe('POST /api/certificates', () => {
    it('should create new certificate', async () => {
      const response = await request(app)
        .post('/api/certificates')
        .set('Authorization', `Bearer ${token}`)
        .send({
          type: 'academic',
          request_data: { purpose: 'Test' }
        });

      expect(response.status).toBe(201);
      expect(response.body.success).toBe(true);
      expect(response.body.data.type).toBe('academic');
    });
  });
});
```

---

## 📝 Pull Request процесс

### Создание Pull Request

1. **Подготовка**:
   - Убедитесь, что ваша ветка обновлена с `develop`
   - Все тесты проходят
   - Код отформатирован
   - Добавлена документация (если нужно)

2. **Создание PR**:
   - Дайте понятное название
   - Опишите изменения
   - Укажите связанные issues (если есть)
   - Добавьте screenshots (для UI изменений)

**Шаблон PR:**

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Связанные issues
Closes #123

## Как протестировать
1. Шаг 1
2. Шаг 2
3. ...

## Чеклист
- [ ] Код соответствует стандартам проекта
- [ ] Добавлены/обновлены тесты
- [ ] Все тесты проходят
- [ ] Обновлена документация
- [ ] Нет конфликтов с develop веткой
```

### Code Review

Ваш PR будет рецензирован мантейнерами проекта. Будьте готовы:
- Ответить на вопросы
- Внести изменения по замечаниям
- Обсудить решения

### Merge

После одобрения PR будет смержен в `develop` ветку.

---

## 🐛 Отчеты об ошибках

### Как создать хороший bug report

1. **Используйте GitHub Issues**
2. **Заполните шаблон issue**:
   - Описание проблемы
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Окружение (ОС, версия Node.js, etc)
   - Screenshots/logs (если применимо)

**Пример:**

```markdown
## Описание
При попытке создать справку получаю ошибку 500

## Шаги для воспроизведения
1. Авторизоваться как студент
2. Перейти на страницу создания справки
3. Заполнить форму
4. Нажать "Отправить"

## Ожидаемое поведение
Справка создается, отображается сообщение об успехе

## Фактическое поведение
Получаю ошибку 500, в консоли:
[Error log]

## Окружение
- ОС: Windows 10
- Node.js: 18.17.0
- Browser: Chrome 120.0
```

---

## 💡 Предложение новых функций

### Feature Request

1. **Проверьте existing issues**: Возможно, кто-то уже предложил это
2. **Создайте issue с тегом "enhancement"**
3. **Опишите**:
   - Проблему, которую решает feature
   - Предлагаемое решение
   - Альтернативные варианты
   - Дополнительный контекст

---

## 📚 Дополнительные ресурсы

### Документация

- [README.md](README.md) - Основная документация
- [DEPLOYMENT.md](DEPLOYMENT.md) - Руководство по развертыванию
- [API_REFERENCE.md](API_REFERENCE.md) - Документация API
- [QUICK_START.md](QUICK_START.md) - Быстрый старт

### Внешние ресурсы

- [MAX Platform Documentation](https://platform.max.ru/docs)
- [Express.js Guide](https://expressjs.com/en/guide/routing.html)
- [React Documentation](https://react.dev/)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/handbook/intro.html)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

---

## 🤔 Вопросы?

- **GitHub Discussions**: Для общих вопросов
- **GitHub Issues**: Для багов и feature requests
- **Email**: [указать контактный email]

---

## 📄 Лицензия

Внося вклад в проект, вы соглашаетесь с тем, что ваш код будет лицензирован под той же лицензией, что и проект.

---

**Спасибо за ваш вклад в MAX Bot! 🎉**

