# 🔐 Политика безопасности

## Поддерживаемые версии

В настоящее время поддерживаются следующие версии проекта:

| Версия | Поддержка          |
| ------ | ------------------ |
| 1.0.x  | :white_check_mark: |
| < 1.0  | :x:                |

## 🚨 Сообщение об уязвимостях

### Как сообщить об уязвимости

Если вы обнаружили уязвимость в безопасности, пожалуйста, **НЕ** создавайте публичный issue.

Вместо этого:

1. **Отправьте email** на [security@your-domain.com] с темой "SECURITY: [краткое описание]"
2. Включите следующую информацию:
   - Тип уязвимости (XSS, SQL Injection, CSRF, etc)
   - Затронутые компоненты (API, Bot, Web)
   - Шаги для воспроизведения
   - Потенциальное влияние
   - Возможные пути исправления (если есть)

3. **Ожидайте ответа** в течение 48 часов
4. Мы проверим уязвимость и свяжемся с вами
5. После исправления мы опубликуем информацию в CHANGELOG

### Что делать после отправки

- ✅ Мы подтвердим получение в течение 48 часов
- ✅ Мы оценим уязвимость и её влияние
- ✅ Мы разработаем и протестируем исправление
- ✅ Мы опубликуем патч в новой версии
- ✅ Мы публично признаем ваш вклад (если вы согласны)

### Координированное раскрытие

Мы просим вас:
- Не раскрывать уязвимость публично до выхода патча
- Дать нам разумное время для исправления (обычно 90 дней)
- Не использовать уязвимость для атак

В обмен мы:
- Оперативно отреагируем на ваше сообщение
- Будем информировать вас о прогрессе
- Публично признаем ваш вклад
- Можем предложить вознаграждение (bug bounty)

---

## 🛡️ Меры безопасности в проекте

### Аутентификация и авторизация

- **JWT токены**: Access + Refresh tokens
- **Время жизни**: Access token - 15 минут, Refresh token - 7 дней
- **Хеширование паролей**: bcrypt с cost factor 10
- **Role-based access control**: student, staff, admin

### Защита данных

- **Валидация входных данных**: Zod schemas
- **Prepared statements**: Защита от SQL Injection
- **CORS**: Настроенные разрешенные origins
- **Helmet.js**: Безопасные HTTP headers
- **Rate limiting**: Ограничение количества запросов (планируется)

### Сетевая безопасность

- **HTTPS**: Обязателен для production
- **SSL/TLS**: Минимум TLS 1.2
- **Secure cookies**: HttpOnly, Secure flags
- **HSTS**: HTTP Strict Transport Security headers

### База данных

- **Шифрование соединений**: SSL для PostgreSQL
- **Минимальные привилегии**: Отдельные пользователи для каждого сервиса
- **Регулярные backup**: Автоматизированное резервное копирование
- **Аудит логов**: Логирование всех операций

### Контейнеры

- **Непривилегированные пользователи**: Контейнеры запускаются не от root
- **Read-only filesystems**: Где возможно
- **Network isolation**: Отдельная Docker сеть
- **Security scanning**: Сканирование образов на уязвимости

---

## 🔒 Рекомендации по безопасной настройке

### 1. Переменные окружения

**❌ НИКОГДА не используйте в production:**
```env
JWT_SECRET=your-secret-key-change-in-production
POSTGRES_PASSWORD=admin123
ADMIN_PASSWORD=admin123
```

**✅ Используйте сложные значения:**
```env
JWT_SECRET=$(openssl rand -base64 64)
POSTGRES_PASSWORD=$(openssl rand -base64 32)
ADMIN_PASSWORD=$(openssl rand -base64 24)
```

### 2. Файрвол

Откройте только необходимые порты:
```bash
# UFW (Ubuntu)
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow 22/tcp   # SSH
sudo ufw allow 80/tcp   # HTTP
sudo ufw allow 443/tcp  # HTTPS
sudo ufw enable
```

### 3. SSH безопасность

```bash
# Отключите вход по паролю
sudo nano /etc/ssh/sshd_config
# PasswordAuthentication no
# PermitRootLogin no

sudo systemctl restart sshd
```

### 4. SSL сертификаты

```bash
# Используйте Let's Encrypt
sudo certbot --nginx -d your-domain.com

# Автоматическое обновление
sudo certbot renew --dry-run
```

### 5. Регулярные обновления

```bash
# Обновляйте систему
sudo apt update && sudo apt upgrade -y

# Обновляйте Docker образы
docker-compose pull
docker-compose up -d

# Обновляйте npm зависимости
npm audit
npm audit fix
```

### 6. Мониторинг

Настройте мониторинг для:
- Неудачных попыток входа
- Необычной активности API
- Ошибок аутентификации
- Превышения rate limits

### 7. Backup

```bash
# Регулярные backup БД
0 2 * * * /opt/max_bot/backup.sh

# Храните backup в отдельном месте
# Шифруйте backup
gpg --symmetric --cipher-algo AES256 backup.sql
```

---

## 🎯 Checklist безопасности для production

### Перед развертыванием

- [ ] Изменены все пароли и секреты по умолчанию
- [ ] Настроен HTTPS с действительным SSL сертификатом
- [ ] Включен файрвол (UFW/firewalld)
- [ ] Отключен root login по SSH
- [ ] Настроена аутентификация по ключам SSH
- [ ] Установлены все security updates ОС
- [ ] Обновлены все npm зависимости
- [ ] Проверены Docker образы на уязвимости
- [ ] Настроены ограничения памяти для контейнеров
- [ ] Настроен мониторинг и алерты

### Конфигурация приложения

- [ ] JWT_SECRET - случайная строка минимум 64 символа
- [ ] JWT_REFRESH_SECRET - случайная строка минимум 64 символа
- [ ] POSTGRES_PASSWORD - сложный пароль минимум 32 символа
- [ ] ADMIN_PASSWORD - сложный пароль минимум 16 символов
- [ ] NODE_ENV=production
- [ ] CORS настроен на конкретные origins
- [ ] Rate limiting включен
- [ ] Логирование настроено
- [ ] Backup автоматизирован

### База данных

- [ ] PostgreSQL доступен только внутри Docker сети
- [ ] Используется отдельный пользователь для приложения
- [ ] Минимальные привилегии для пользователя
- [ ] SSL соединение включено
- [ ] Настроены connection limits
- [ ] Регулярные backup настроены
- [ ] Тестовое восстановление из backup выполнено

### Nginx

- [ ] SSL сертификат установлен
- [ ] HTTP перенаправляет на HTTPS
- [ ] HSTS header включен
- [ ] Security headers настроены
- [ ] Rate limiting настроен
- [ ] Gzip compression включен
- [ ] Access/Error логи настроены

### Мониторинг

- [ ] Health checks настроены
- [ ] Логи централизованы
- [ ] Алерты настроены для критических событий
- [ ] Метрики собираются (CPU, Memory, Disk)
- [ ] Uptime мониторинг настроен
- [ ] Backup мониторинг настроен

---

## 🔍 Известные уязвимости

### В текущей версии

На данный момент известных критических уязвимостей нет.

### Исправленные уязвимости

История исправлений будет опубликована в CHANGELOG после обнаружения и устранения.

---

## 📚 Ресурсы по безопасности

### Документация

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Node.js Security Best Practices](https://nodejs.org/en/docs/guides/security/)
- [Docker Security](https://docs.docker.com/engine/security/)
- [PostgreSQL Security](https://www.postgresql.org/docs/current/security.html)

### Инструменты

- **npm audit**: Проверка уязвимостей в зависимостях
- **Snyk**: Автоматизированное сканирование
- **Trivy**: Сканирование Docker образов
- **OWASP ZAP**: Тестирование безопасности веб-приложений

### Команды для проверки

```bash
# Проверка npm зависимостей
npm audit

# Проверка Docker образов
docker scan max_bot_api:latest

# Проверка SSL конфигурации
sslscan your-domain.com

# Проверка headers
curl -I https://your-domain.com
```

---

## 🏆 Bug Bounty Program

### Вознаграждения

Мы ценим работу security researchers и предлагаем вознаграждения:

| Серьезность | Описание | Вознаграждение |
|------------|----------|----------------|
| 🔴 Critical | RCE, SQL Injection, Authentication bypass | $500-$1000 |
| 🟠 High | XSS, CSRF, Sensitive data exposure | $200-$500 |
| 🟡 Medium | Information disclosure, DoS | $50-$200 |
| 🟢 Low | Minor security issues | Признание в CHANGELOG |

### Scope

**В scope:**
- Web приложение (your-domain.com)
- API endpoints (your-domain.com/api)
- MAX Bot
- Публичные Docker образы

**Вне scope:**
- DoS/DDoS атаки
- Social engineering
- Физический доступ
- Third-party сервисы (MAX Platform, etc)

---

## 📞 Контакты

- **Security Email**: security@your-domain.com
- **PGP Key**: [Ваш PGP ключ]
- **Response Time**: В течение 48 часов

---

## 📄 Responsible Disclosure Policy

Мы придерживаемся политики ответственного раскрытия информации:

1. Исследователь сообщает об уязвимости приватно
2. Мы подтверждаем получение в течение 48 часов
3. Мы оцениваем и исправляем уязвимость
4. Мы информируем исследователя о прогрессе
5. После выхода патча публикуется информация
6. Исследователь получает признание (если согласен)

Стандартное время на исправление:
- **Critical**: 7 дней
- **High**: 30 дней
- **Medium**: 60 дней
- **Low**: 90 дней

---

## 🙏 Благодарности

Благодарим следующих security researchers:

- [Ваше имя] - [Описание уязвимости] - [Дата]

*(Список будет обновляться по мере поступления сообщений)*

---

**Последнее обновление**: 15 ноября 2025

**Версия политики**: 1.0

