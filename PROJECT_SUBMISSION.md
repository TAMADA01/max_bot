# Проект MAX Bot - Система управления заявками на справки

## 📌 Информация о проекте

**Название проекта:** MAX Bot  
**Тип:** Система управления заявками на справки для учебных заведений  
**Версия:** 1.0.0  
**Дата:** 15 ноября 2025  

---

## 🔗 Ссылка на GitHub репозиторий

```
https://github.com/your-username/max_bot
```

**Альтернативная ссылка для клонирования:**
```bash
git clone https://github.com/your-username/max_bot.git
```

---

## 📋 Описание проекта

MAX Bot - это комплексное решение для автоматизации процесса подачи и обработки заявок на справки в учебных заведениях. Система состоит из трех основных компонентов:

1. **REST API Server** - Backend сервер на Node.js + Express.js + TypeScript
2. **MAX Bot** - Интеграция с платформой MAX для взаимодействия через мессенджер
3. **Web Interface** - React приложение для управления заявками через браузер

### Основные возможности

- ✅ Подача заявок на справки через бота или веб-интерфейс
- ✅ Отслеживание статуса заявок в реальном времени
- ✅ Управление пользователями (студенты, сотрудники, администраторы)
- ✅ 5 типов справок (enrollment, academic, attendance, graduation, other)
- ✅ Автоматизированная обработка заявок
- ✅ REST API с документацией Swagger
- ✅ JWT аутентификация и авторизация
- ✅ Полная контейнеризация с Docker

---

## 🛠️ Технологический стек

### Backend
- **Runtime:** Node.js 18
- **Framework:** Express.js 4.18
- **Language:** TypeScript 5.3
- **Database:** PostgreSQL 15
- **Cache:** Redis 7
- **Auth:** JWT (jsonwebtoken)
- **Validation:** Zod
- **Documentation:** Swagger UI

### Bot
- **Platform:** MAX Platform (@maxhub/max-bot-api)
- **HTTP Client:** Axios
- **Localization:** Multi-language (RU, EN)

### Frontend
- **Framework:** React 18
- **Build Tool:** Vite 7
- **Styling:** TailwindCSS 4
- **UI Library:** @maxhub/max-ui
- **Routing:** React Router DOM 7

### DevOps
- **Containerization:** Docker + Docker Compose
- **Reverse Proxy:** Nginx
- **Migrations:** SQL migrations
- **CI/CD:** GitHub Actions ready

---

## 📦 Структура репозитория

```
max_bot/
├── api/                       # Backend API сервер
│   ├── src/                  # Исходный код
│   ├── Dockerfile            # Docker образ для API
│   └── package.json          # Зависимости npm
│
├── bot/                       # MAX Bot
│   ├── src/                  # Исходный код бота
│   ├── Dockerfile            # Docker образ для бота
│   └── package.json          # Зависимости npm
│
├── web/                       # Web интерфейс
│   ├── src/                  # React приложение
│   ├── Dockerfile            # Docker образ для web
│   └── package.json          # Зависимости npm
│
├── nginx/                     # Nginx конфигурация
│   └── nginx.conf            # Reverse proxy config
│
├── docker-compose.yml         # Docker Compose конфигурация
├── requirements.txt           # Полный список зависимостей
├── SETUP_GUIDE.md            # Инструкция по запуску
├── README.md                 # Основная документация
└── example-env               # Пример переменных окружения
```

---

## 📚 Документация в репозитории

В репозитории доступна полная документация:

| Файл | Описание |
|------|----------|
| **README.md** | Основная документация проекта (на русском) |
| **README.en.md** | English version of documentation |
| **SETUP_GUIDE.md** | Подробная инструкция по запуску |
| **requirements.txt** | Полный список всех библиотек и версий |
| **QUICK_START.md** | Быстрый старт за 5 минут |
| **DEPLOYMENT.md** | Руководство по развертыванию |
| **API_REFERENCE.md** | Документация REST API |
| **ADMIN_GUIDE.md** | Руководство администратора |
| **CONTRIBUTING.md** | Руководство для разработчиков |
| **FAQ.md** | Часто задаваемые вопросы |
| **SECURITY.md** | Политика безопасности |
| **CHANGELOG.md** | История изменений |

---

## 🚀 Быстрый запуск

### Системные требования

- Docker 20.10+
- Docker Compose 2.0+
- Git

### Инструкция по запуску

```bash
# 1. Клонировать репозиторий
git clone https://github.com/your-username/max_bot.git
cd max_bot

# 2. Создать файл окружения
cp example-env .env

# 3. Отредактировать .env (указать MAX_BOT_TOKEN и пароли)
# Windows: notepad .env
# Linux/Mac: nano .env

# 4. Запустить все сервисы
docker-compose up -d

# 5. Проверить статус
docker-compose ps

# 6. Просмотреть логи
docker-compose logs -f
```

### Доступ к сервисам

После запуска сервисы будут доступны по адресам:

- **Web Interface:** http://localhost:3000
- **API Server:** http://localhost:8080
- **Swagger UI:** http://localhost:8080/api-docs
- **Admin Panel:** http://localhost:8080/admin

**Учетные данные по умолчанию:**
- Email: `admin@test.com`
- Пароль: `admin123`

---

## 🔧 Команды Docker

### Основные операции

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Просмотр логов
docker-compose logs -f

# Просмотр статуса
docker-compose ps

# Перезапуск сервиса
docker-compose restart api

# Пересборка образов
docker-compose build

# Полная переустановка (удаляет данные!)
docker-compose down -v
docker-compose up -d --build
```

### Сборка отдельных образов

```bash
# API Server
docker build -t max-bot-api:latest ./api

# Bot
docker build -t max-bot-bot:latest ./bot

# Web Interface
docker build -t max-bot-web:latest ./web
```

### Запуск контейнеров

```bash
# Сначала запустить инфраструктуру
docker-compose up -d postgres redis

# Затем приложения
docker-compose up -d api bot web
```

---

## 📝 Requirements.txt - Описание библиотек

В репозитории находится файл **`requirements.txt`** с полным списком всех зависимостей проекта:

### API Server (Node.js)

**Production зависимости:**
- `express@4.18.2` - Web framework
- `pg@8.11.3` - PostgreSQL клиент
- `redis@4.6.12` - Redis клиент
- `jsonwebtoken@9.0.2` - JWT аутентификация
- `bcryptjs@2.4.3` - Хеширование паролей
- `zod@3.22.4` - Валидация данных
- `swagger-ui-express@5.0.1` - API документация

**Development зависимости:**
- `typescript@5.3.3` - TypeScript компилятор
- `ts-node@10.9.1` - TypeScript runtime
- `nodemon@3.0.2` - Auto-reload при разработке

### MAX Bot

**Production зависимости:**
- `@maxhub/max-bot-api@0.2.1` - MAX Bot SDK
- `axios@1.6.2` - HTTP клиент
- `express@4.18.2` - Web framework для webhook

### Web Interface

**Production зависимости:**
- `react@18.2.0` - React библиотека
- `vite@7.2.1` - Build tool
- `tailwindcss@4.1.17` - CSS framework
- `react-router-dom@7.9.5` - Routing
- `@maxhub/max-ui@0.1.12` - UI компоненты

### Docker образы

- `postgres:15` - База данных
- `redis:7-alpine` - Кэш
- `node:18-alpine` - Runtime для приложений
- `nginx:alpine` - Reverse proxy (опционально)

**Полный список с версиями доступен в файле `requirements.txt`**

---

## 🗄️ База данных

### Схема базы данных

Проект использует PostgreSQL 15 со следующими таблицами:

1. **users** - Пользователи системы
2. **students** - Данные студентов
3. **staff** - Данные сотрудников
4. **certificates** - Заявки на справки
5. **certificate_files** - Файлы, прикрепленные к справкам

### Миграции

Миграции выполняются автоматически при запуске API сервера. Файлы миграций находятся в:
```
api/src/database/migrations/
├── 001_initial_schema.sql
└── 002_add_certificate_files.sql
```

---

## 🔒 Безопасность

### Реализованные меры безопасности

- ✅ JWT токены для аутентификации (access + refresh)
- ✅ Bcrypt хеширование паролей (cost factor 10)
- ✅ Валидация входных данных (Zod schemas)
- ✅ Защита от SQL injection (prepared statements)
- ✅ CORS конфигурация
- ✅ Helmet.js security headers
- ✅ Разделение прав доступа (student, staff, admin)

### Рекомендации для production

⚠️ **Обязательно измените в production:**
- `JWT_SECRET` - минимум 64 символа
- `JWT_REFRESH_SECRET` - минимум 64 символа
- `POSTGRES_PASSWORD` - сложный пароль
- `ADMIN_PASSWORD` - сложный пароль

Подробнее в файле **SECURITY.md** в репозитории.

---

## 📊 API Endpoints

### Основные эндпоинты

#### Аутентификация
- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/refresh` - Обновление токена
- `GET /api/auth/me` - Текущий пользователь

#### Пользователи
- `GET /api/users` - Список пользователей
- `GET /api/users/:id` - Получить пользователя
- `PUT /api/users/:id` - Обновить пользователя
- `DELETE /api/users/:id` - Удалить пользователя

#### Справки
- `GET /api/certificates` - Список справок
- `POST /api/certificates` - Создать заявку
- `GET /api/certificates/:id` - Получить справку
- `PUT /api/certificates/:id` - Обновить справку
- `PATCH /api/certificates/:id/status` - Изменить статус
- `DELETE /api/certificates/:id` - Удалить справку

**Интерактивная документация доступна в Swagger UI:** http://localhost:8080/api-docs

---

## 📈 Примеры использования через командную строку

### Пример 1: Регистрация и вход

```bash
# Регистрация нового студента
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "student@example.com",
    "password": "password123",
    "first_name": "Иван",
    "last_name": "Иванов",
    "role": "student",
    "student_data": {
      "student_id": "2024-0001",
      "group_name": "ИТ-401",
      "faculty": "ФИТ",
      "specialty": "Программная инженерия",
      "year_of_study": 4
    }
  }'

# Вход в систему
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "student@example.com",
    "password": "password123"
  }'
```

### Пример 2: Создание заявки на справку

```bash
# Сохранить токен из предыдущего запроса
TOKEN="your_access_token_here"

# Создать заявку
curl -X POST http://localhost:8080/api/certificates \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "academic",
    "request_data": {
      "purpose": "Для военкомата",
      "copies": 2,
      "delivery_method": "pickup"
    }
  }'
```

### Пример 3: Просмотр списка заявок

```bash
# Все заявки
curl -X GET "http://localhost:8080/api/certificates" \
  -H "Authorization: Bearer $TOKEN"

# Заявки в статусе pending
curl -X GET "http://localhost:8080/api/certificates?status=pending" \
  -H "Authorization: Bearer $TOKEN"
```

---

## 🧪 Тестирование

### Health Check

```bash
# Проверить работу API
curl http://localhost:8080/health

# Ожидаемый ответ:
# {"status":"OK","service":"api","timestamp":"2025-11-15T12:00:00.000Z"}
```

### Подключение к базе данных

```bash
# Через Docker
docker-compose exec postgres psql -U admin -d bot_max

# Примеры SQL запросов:
# SELECT COUNT(*) FROM users;
# SELECT * FROM certificates WHERE status = 'pending';
```

---

## 🔄 Обновление проекта

```bash
# 1. Создать backup базы данных
docker-compose exec postgres pg_dump -U admin bot_max > backup.sql

# 2. Обновить код
git pull origin main

# 3. Остановить сервисы
docker-compose down

# 4. Пересобрать и запустить
docker-compose up -d --build

# 5. Проверить работу
docker-compose logs -f
curl http://localhost:8080/health
```

---

## 📞 Контакты и поддержка

### Документация

Вся документация доступна в репозитории:
- **Основная:** README.md
- **API:** API_REFERENCE.md
- **Установка:** SETUP_GUIDE.md
- **FAQ:** FAQ.md

### Получение помощи

- **GitHub Issues:** Для сообщений об ошибках
- **GitHub Discussions:** Для вопросов
- **Email:** [указать контактный email]

### Swagger UI

Интерактивная документация API: http://localhost:8080/api-docs

---

## ✅ Checklist для проверки

- [ ] Docker и Docker Compose установлены
- [ ] Репозиторий склонирован с GitHub
- [ ] Файл .env создан и настроен
- [ ] Все сервисы запущены (`docker-compose ps`)
- [ ] API отвечает на health check
- [ ] Web интерфейс открывается в браузере
- [ ] Swagger UI доступен
- [ ] Можно войти с учетными данными администратора
- [ ] Можно создать тестовую заявку

---

## 📄 Лицензия

[Укажите вашу лицензию]

---

## 🙏 Благодарности

- MAX Platform - за предоставление платформы для ботов
- Все участники проекта

---

**Дата создания:** 15 ноября 2025  
**Версия:** 1.0.0  
**Статус:** Готов к использованию

---

## 💡 Примечания

1. **Для локальной разработки** используйте детальную инструкцию в файле **SETUP_GUIDE.md**
2. **Для production развертывания** см. **DEPLOYMENT.md**
3. **Для разработчиков** см. **CONTRIBUTING.md**
4. **Полный список зависимостей** в файле **requirements.txt**

---

**Этот документ можно конвертировать в DOCX для предоставления.**

Для конвертации используйте:
- **Pandoc**: `pandoc PROJECT_SUBMISSION.md -o PROJECT_SUBMISSION.docx`
- **Онлайн конвертеры**: markdowntoword.com
- **VS Code**: Расширение "Markdown PDF" или "Docs Markdown"

---

**Ссылка на GitHub:** https://github.com/your-username/max_bot

